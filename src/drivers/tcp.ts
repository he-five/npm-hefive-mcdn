import {McdnCmd, ServiceCommands} from "./mcdn-cmd";


class Tcp {
    constructor () {}
    public connect () {}
    public disconnect () {}
    public sendCmd(cmd : McdnCmd ) {}
    //public sendStr(cmd : string){}

}

export { Tcp }
